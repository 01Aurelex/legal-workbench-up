{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "法岩律师本地工作台默认权限（仅单实例与窗口，后端由外壳直接拉起，不开放 shell）",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
